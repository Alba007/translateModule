/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { KeyPipe } from './key.pipe';

describe('Pipe: Keye', () => {
  it('create an instance', () => {
    let pipe = new KeyPipe();
    expect(pipe).toBeTruthy();
  });
});
